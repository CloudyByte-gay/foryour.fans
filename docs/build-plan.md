# Build Plan

Companion to [`prompts/full.md`](../prompts/full.md), the phase-by-phase spec fed to Codex. That file is the source of truth for *what* to build in each phase; this file is the project-tracking view — one row per phase, in build order, with the exit criteria and open decisions that don't belong in the spec itself.

The backend/API is specced in `prompts/full.md`; the web client (`apps/web`) has its own phase-by-phase spec in [`prompts/web.md`](../prompts/web.md), numbered `WEB PHASE 0`–`17` to shadow the API phases it consumes. See [Web UI build plan](#web-ui-build-plan) below.

Two decisions locked in during spec review (2026-09-01), both reflected in `prompts/full.md`:

- **Hosting model**: the app never operates its own PDS. Users bring their own AT Protocol identity; public records are written to *their* repo via OAuth-scoped writes, never to an app-controlled one.
- **Content policy**: adult/NSFW creator content is in scope. This rules out assuming Stripe as the eventual `PaymentProvider`/`PayoutProvider`, and pulls creator KYC/age-verification forward from "future work" to a gating design requirement (Phase 14, enforced starting Phase 6's payout onboarding).

Each phase below ends with the standard exit checklist from `prompts/full.md`: tests pass, lint passes, type check passes, app starts, README updated, `docs/architecture.md` updated, files-changed summary, known limitations documented, next phase identified. Not repeated per-row.

**Phase 11 (AT Protocol Spaces) has been extracted** from `prompts/full.md` / `prompts/web.md` into `prompts/atproto-spaces.md` and now runs **dead last** — after Phases 12–17 and after the two rearchitecture specs (`prompts/creator-owned-pds.md`, then `prompts/bluesky-public-posts.md`). Slot 11 is left vacant, not renumbered. See ["Planned rearchitecture"](#planned-rearchitecture-creator-owned-pds-storage--bluesky-compatible-public-posts) below for the full order and how the rearchitecture specs change Phases 12–17.

**Status (2026-09-02): Phases 1–10 done.** Phase 2 verified end-to-end against the live AT network: real handle resolution (`bsky.app`), a real Pushed Authorization Request to `bsky.social`, and a full fake-OAuth-client callback→session→cookie round trip (12 automated tests, including identity creation vs. returning-user login, CSRF-protected logout). Phase 3 lexicons (`fans.foryour.profile/post/tier`) are authored, generated, and validated (16 tests). Phase 4 wires `fans.foryour.profile` into real routes for the first time (`POST /creators`, `PATCH /creators/me`) with AT-write-before-DB-write ordering, slug validation/cooldown, and ownership-by-construction (`/creators/me` never accepts a creator id from the client) — 15 route tests plus a caught-and-fixed real bug (the slug-change cooldown originally blocked a creator's very first edit). `/become-a-creator`, `/creator/settings`, `/c/[slug]` are live. Phase 5 adds `SubscriptionTier` CRUD (`POST/PATCH/DELETE /creators/me/tiers`, `GET /creators/:creator/tiers`) with the same AT-write-first discipline, reusing one generated `tid` rkey across a tier's updates/deletion rather than minting a new one each time — 16 route tests + 8 pure-validation unit tests, plus a mid-phase architecture correction (tier domain logic moved from `apps/api` into `packages/subscriptions`, and the AT-record injection types into `packages/atproto`, once it became clear a package can't depend on an app). The Lexicon namespace was also renamed from the `dev.creator` placeholder to the real `fans.foryour` (reverse-DNS of the now-selected production domain, `foryour.fans`) — zero-migration since nothing had been published under the placeholder yet. Phase 6 builds `PaymentProvider`/`PayoutProvider` interfaces (hosted-checkout shape: `createSubscription` returns `{status: "pending"|"active", redirectUrl?}` rather than assuming synchronous confirmation) plus fake implementations wired as the real Phase 6 code (never a real processor — real money must never move through them), `Subscription`/`PaymentEvent`/`PayoutAccount` models entirely in Postgres (never AT Protocol — see docs/atproto-vs-database.md), idempotent webhook processing (`PaymentEvent` ledger keyed by `[provider, providerEventId]`, verified by a real replay test), and an entitlement service (`canAccess`, using `SubscriptionTier.sortOrder` as an inferred tier hierarchy pending Phase 7 confirmation) — continuing Phase 5's package-boundary discipline (`packages/subscriptions` owns the domain logic, `apps/api` routes stay thin). Two real test-hygiene bugs were found and fixed along the way: a hardcoded webhook event ID that collided across isolated vs. full-suite runs, and a genuine cross-file test-handle collision (`"liam.test"` used in both `creators.test.ts` and `subscriptions.test.ts`) that caused nondeterministic failures under vitest's parallel file execution — fixed with a `uniqueHandle()` helper, documented in docs/architecture.md. Phase 7 builds `ContentRepository` (storage-only by design — entitlement checking stays outside it, in `apps/api/src/routes/posts.ts`, per `canAccess`'s own Phase-6-era doc comment promising exactly that) and `PrivateContentRepository` in a new `packages/content`, plus the `Post`/`PostMedia` Postgres models. Only `PUBLIC`-visibility posts get a mirrored `fans.foryour.post` AT record (reusing the same mint-once/reuse-rkey/retract-on-leaving-PUBLIC discipline Phase 5 established for tiers); `SUBSCRIBERS`/`TIER` posts never touch the AT network at all. `updatePost` is fully implemented (including the visibility-crossing publish/retract transition logic) despite Phase 7 defining no HTTP route for it — spec's route list has no `PATCH` — so it's covered by direct repository tests instead of route tests. The required (but intentionally unfinished) `AtprotoSpacesContentRepository` stub is also in place, wired nowhere. All 5 of the spec's access-control test cases pass (anon, non-subscriber, valid subscriber, wrong-tier, creator-self), exercised through the real subscribe+webhook HTTP flow, not hand-inserted rows. Phase 8 builds `ObjectStorage` (`packages/media`, deliberately storage-only, same discipline as `ContentRepository`) with `S3ObjectStorage` as the real, only implementation — unlike Phase 6's fakes, object storage has no business-decision blocker (MinIO is already a real running S3-compatible target), so it's genuinely wired against local MinIO in `server.ts`, and verified with a real presigned-URL PUT/GET/delete round trip against it (byte-for-byte match, 404 confirmed after delete) — the Phase 8 analogue of Phase 2's live-network check. `MediaAsset`'s `PENDING_UPLOAD → PROCESSING → READY/REJECTED` lifecycle is driven by an injected `MediaProcessor`; `PassthroughMediaProcessor` (always "ready", no real scanning) is the real Phase 8 implementation, with the interface itself being the hook Phase 14's real scanner plugs into. `GET /media/:id/access`'s entitlement rule (creator-always, else any active subscriber at any tier) is narrower than a post's because Phase 8 doesn't attach a `MediaAsset` to a specific `Post` yet — `PostMedia` gained a real FK to `MediaAsset` but still has no writer. Along the way, a real, previously-undetected gap surfaced: `apps/api`'s `pnpm typecheck` had never covered `test/` (only `src`) since Phase 1, silently — fixed with a dedicated `tsconfig.typecheck.json`, which immediately surfaced (and let a fix be verified against) real errors at every `buildApp()` call site needing the new options. A rare (~4%), unreproduced test flake was also observed and honestly documented rather than papered over with a guessed fix. Phase 9 builds `GET /feed` (the home feed) and `GET /creators/:creator/feed` (a per-creator feed). Since no `Follow` model exists anywhere in `prompts/full.md`'s 17 phases, "public posts from followed/discovered creators" reads as "every `PUBLIC` post platform-wide" — a faithful reading, not a shortcut, since `PUBLIC` already means visible to anyone. `ContentRepository` grew a sixth method, `getFeed` (an unfiltered candidate set, same storage-only discipline as the original five), with real entitlement filtering staying in `apps/api/src/routes/feed.ts` via the same `checkPostAccess` Phase 7 built (exported and reused, not re-implemented). `GET /feed` has `?limit=` only (an honest, documented over-fetch-and-slice, not a strict-guarantee retry loop, since Phase 9's own spec text doesn't ask for cursor pagination); `GET /creators/:creator/feed` gets real cursor pagination, since it never drops a row — an inaccessible post becomes a safe `{id, visibility, createdAt, requiredTier, locked: true}` stub instead of being omitted. Phase 10 builds a new `packages/discovery`: `JetstreamIngestor` (a resilient WebSocket client, own reconnect/backoff, own `apps/api/src/ingest.ts` process — deliberately separate from `server.ts` so N HTTP replicas don't each re-consume the same firehose), a commit-event parser + indexer feeding `IndexedCreatorProfile`/`IndexedPost`/`IndexedTier` (DID-keyed, covering any DID publishing `fans.foryour.*` records, not only ones registered here), and `GET /discover`/`GET /search`. The spec's own "research current mechanisms" instruction paid off directly: doc-based research described a "v2" Jetstream wire format, but connecting to the real live production endpoint showed it actually serves the older flat v1 shape (`{did, time_us, kind: "commit", commit: {...}}`) with a `wantedCollections` query param (not `collections`, which the real server silently ignores rather than erroring on) — `packages/discovery` is built against the verified-real format, confirmed by a live `JetstreamIngestor` connecting to production and processing hundreds of real events end-to-end (not just the raw parser). A second, real cross-file test race was found and fixed the same day (three new `packages/discovery` test files sharing a table, one file's blanket `afterEach` deleteMany wiping another's in-progress test data) — same root cause as Phase 6's handle-collision bug, fixed the same way (scope cleanup to specific dids, never a blanket wipe). 294 tests total across the workspace. Phases 12–17 not started; the old Phase 11 (Spaces) is extracted to `prompts/atproto-spaces.md` and runs dead last.

| # | Phase | Builds | Exit criteria (beyond the standard checklist) | Depends on |
|---|-------|--------|------------------------------------------------|------------|
| 1 | Repository Foundation | pnpm monorepo, Fastify API skeleton, Prisma `User` model, Docker Compose (Postgres/Redis/MinIO), Next.js shell, CI workflow | `/health` and `/ready` pass against real Postgres; CI runs lint/typecheck/test/build on PR | — |
| 2 | AT Identity & OAuth | Handle→DID→PDS resolution, AT OAuth (PKCE + DPoP), server-side sessions | Returning-user login test passes; no app passwords or Bluesky app passwords ever stored | 1 |
| 3 | Custom Lexicons | `fans.foryour.profile/post/tier` lexicons, generated types, blob-upload path to user's own PDS | Validation tests pass; `docs/architecture.md` states which fields live in AT vs. our DB | 2 |
| 4 | Creator Accounts | `Creator` (no separate `CreatorProfile` table needed — see docs/atproto-vs-database.md), ~~slug rules + reserved words + cooldown~~ (removed by the Handle-as-Identity refactor below), ownership-by-construction | A user cannot PATCH another creator's account (test) | 2, 3 |
| 5 | Subscription Tiers | `SubscriptionTier` CRUD in `packages/subscriptions` (not `apps/api` — see docs/architecture.md), reused `atRkey` per tier, deactivation instead of deletion | Deactivating a tier doesn't break existing subscribers' historical data; a tier's AT record is updated in place across edits, never duplicated (test) | 4 |
| 6 | Subscription & Payment Abstraction | `PaymentProvider`/`PayoutProvider` interfaces in `packages/subscriptions` + fake implementations, `Subscription`/`PaymentEvent`/`PayoutAccount` models with price snapshot fields, `canAccess` entitlement service, `POST/GET/PATCH` subscription routes, public `POST /webhooks/:provider`, payout onboarding routes | Idempotent webhook test passes (replay same event twice → `"processed"` then `"duplicate"`, one DB effect); `canAccess` uses the snapshotted `priceCentsAtSubscription`, not live tier price; cancel actually calls the provider (spy-verified) | 5 |
| 7 | Private Content Architecture | `ContentRepository` interface + `PrivateContentRepository` (Postgres; storage-only, no entitlement logic) in new `packages/content`, `Post`/`PostMedia` models, `POST/GET/DELETE` post routes gated via `canAccess`, `AtprotoSpacesContentRepository` stub | All 5 access-control test cases in the spec pass (anon, non-subscriber, valid subscriber, wrong-tier, creator-self); PUBLIC-visibility-transition AT publish/retract logic verified directly | 6 |
| 8 | Secure Media | `ObjectStorage` interface + `S3ObjectStorage` (real, S3-compatible) in new `packages/media`, presigned upload/download flow (`POST /media/upload-url`, `POST /media/:id/complete`, `GET /media/:id/access`), `MediaAsset` with explicit status lifecycle (`PENDING_UPLOAD→PROCESSING→READY`/`REJECTED`), `MediaProcessor` hook (+ `PassthroughMediaProcessor`) | `GET /media/:id/access` never issues a signed URL for a non-`READY` asset, even to an entitled subscriber (test); real presigned PUT/GET round trip verified against MinIO | 7 |
| 9 | Feeds | `GET /feed` (home feed, PUBLIC platform-wide + unlocked subscriptions), `GET /creators/:creator/feed` (cursor-paginated, locked-post metadata-only stubs), `ContentRepository.getFeed` | Locked-post payload contains no body/media data (test); cursor pagination returns every post exactly once, in order | 7, 8 |
| 10 | AT Public Discovery | New `packages/discovery`: `JetstreamIngestor` (own process, `apps/api/src/ingest.ts`), commit-event parser + indexer, `IndexedCreatorProfile`/`IndexedPost`/`IndexedTier`, `GET /discover`, `GET /search` | A record deleted upstream (a `delete` commit) removes the indexed row, not just creates/updates get applied (test); real live Jetstream connectivity verified against production | 3, 9 |
| ~~11~~ | **Extracted** → [`prompts/atproto-spaces.md`](../prompts/atproto-spaces.md) | AT Protocol Spaces adapter (`AtprotoSpacesContentRepository`, `SpaceAuthority`, `ATPROTO_SPACES_ENABLED=false`) — moved out of the numbered sequence, runs **dead last** (after 12–17 and the two rearchitecture specs). Slot left vacant, not renumbered. | 7, rearchitecture |
| 12 | Comments, Likes, Social | `Comment`/`Like`, access inherited from parent post | Comment on tier-gated post is itself gated the same way (test) | 7 |
| 13 | Creator Dashboard | Revenue/subscriber analytics, ownership-protected | MRR computed from billing DB, not AT records; uses price snapshot from Phase 6 | 6, 6-payout |
| 14 | Trust & Safety Foundation | `Report`/`ModerationCase`/`ContentLabel`/`UserBlock`/`AuditLog`, verification-status field on `Creator`, moderation gates on payout/adult-content posting | Real (non-fake) `PaymentProvider`/`PayoutProvider` cannot be enabled while verification status is unresolved (design-level gate, documented) | 6, 8 |
| 15 | Production Hardening | Rate limiting, security headers, DB constraints/indexes, webhook replay/out-of-order tests, `docs/security.md`, `docs/threat-model.md`, `docs/production-readiness.md` | Documented attempt to bypass signed URLs / tier restrictions fails | all above |
| 16 | Kubernetes Deployment | Manifests (base + overlays), no in-cluster Postgres/S3 by default | Secrets never committed; readiness/liveness probes present for API and Web | 15 |
| 17 | Architecture Review | `docs/final-architecture.md` with the 5 required diagrams, risk list, MVP-readiness classification | No new features added — review only | all above |

## Known deliberate gaps (not scheduled in any phase above)

Carried over from the spec review — intentionally out of scope of `prompts/full.md`, called out so they aren't mistaken for oversights later:

- **Transactional email/notifications** (receipts, cancellation confirmations, payout failures, moderation notices). Needed before real-money production launch; not in any phase.
- **Real payment/payout processor selection.** Phase 6 builds the abstraction and a fake implementation only; picking an actual adult-content-compatible processor is a business decision, not a Codex phase.
- **Video transcoding, thumbnailing, virus/moderation scanning implementations.** Phase 8 designs the interface (`MediaProcessor`) and status lifecycle (`PENDING_UPLOAD→PROCESSING→READY`/`REJECTED`), shipping `PassthroughMediaProcessor` (always "ready", no real scanning) as the real implementation; it does not implement the scanning/transcoding itself — that's Phase 14's job, plugging into the same interface with zero schema change.
- **Frontend/e2e test tooling** (e.g. Playwright). `prompts/full.md` only requires API-level tests; `prompts/web.md` fills this gap — Vitest + React Testing Library from `WEB PHASE 0`, Playwright from `WEB PHASE 2` once the auth flow exists.

## Out-of-band refactor: Handle as Identity ([`prompts/handle-identity.md`](../prompts/handle-identity.md))

Runs **after PHASE 7 / WEB PHASE 4, before PHASE 8 / WEB PHASE 5** — its own session(s), backend then web. It removes the `Creator.slug` PHASE 4 introduced (an app-owned vanity string layered on portable identity — the only such name in the system). After it: the **AT handle is the public identity** for every user, the **DID is the durable key**, the creator page is `/c/<handle>` (and `/c/<did>`, which never breaks), a changed handle 301-redirects old `/c/<oldhandle>` links via a new `CreatorHandleHistory`, and `POST /creators` takes profile fields only. `prompts/full.md` PHASE 8+ and `prompts/web.md` WEB PHASE 5+ already assume no slug exists; PHASE 4 / WEB PHASE 4 carry a "partly superseded" note.

**Backend refactor done (2026-09-02).** Migration `20260902020000_handle_as_identity` drops `Creator.slug` / `slugUpdatedAt` and their unique index, adds `CreatorHandleHistory` (`creatorId`, denormalized `did`, `previousHandle`, `recordedAt`; `@@index` on `previousHandle` and `did`). `apps/api/src/services/creators.ts` lost every slug symbol (`SLUG_PATTERN`, `RESERVED_SLUGS`, `validateSlug`, the `Slug*Error` classes, the 7-day cooldown); `POST /creators` and `PATCH /creators/me` are profile-only. New `resolveCreatorByIdentifier` returns `found` / `moved` / `not-found`; `GET /creators/:identifier` answers a former handle with `301 { movedTo, did }` + `Location`. `syncUserFromProfile` (`packages/auth`) appends a `CreatorHandleHistory` row when it overwrites a creator's handle at login — no new hot-path network calls. `SubscriptionWithContext` / `/subscriptions` now carry `creator.handle` instead of `creator.slug`.

**Web refactor done (2026-09-02).** `/become-a-creator` is a 3-step wizard (Profile → Content rating → Review); the Slug step, `apps/web/lib/slug.ts`, `lib/useSlugAvailability.ts` and their tests are gone. `app/(marketing)/c/[slug]` → `app/(marketing)/c/[handle]`; the page handles `200` (render), `301 {movedTo}` (a former handle → `permanentRedirect('/c/<movedTo>')`, an HTTP 308) and `404`. `/creator/settings` lost the `SlugChangeDialog`; its Page-address card is a static note (address follows the AT handle; `/c/<did>` never changes). `lib/session.ts#OwnCreatorSummary`, the dashboard and become-a-creator redirects all key on `handle` (falling back to `did`). Playwright: the become-a-creator spec no longer fills a slug and asserts landing on `/c/<fixture-handle>`; a new spec drives `POST /__e2e__/simulate-handle-change` on the fake API and asserts `/c/<oldhandle>` → 308 → `/c/<newhandle>`.

`pnpm build` / `-r lint` / `-r typecheck` / `-r test` all green (api 103, web 41, content 22, subscriptions 8, lexicons 16); `pnpm --filter @foryour-fans/web test:e2e` green (8 Playwright). `grep -ri slug apps packages` is clean.

## Planned rearchitecture: creator-owned PDS storage + Bluesky-compatible public posts

Two specs were added after Phase 10 that change assumptions every later phase (12–17 / WEB 12–17) is written against, plus a third that carves the experimental Spaces work out of the numbered sequence entirely. Each runs as its own session(s) with the same discipline as the Handle-as-Identity refactor above (tests green, exit checklist, `Stop after` line).

**Status (2026-09-02): `bluesky-public-posts.md` — implemented, flag-gated.** Research output `docs/bluesky-public-posts.md` written from primary sources (vendored `packages/lexicons/vendor/app/bsky/**`). New `packages/atproto/src/bskyPost.ts` (facet parser with UTF-8 byte offsets, `buildBskyPostRecord` + validator, embed builders). `CreatorOwnedContentRepository` now writes a lexicon-accurate `app.bsky.feed.post` (was `{text, createdAt}`), reuses `atRkey` + new `Post.bskyRkey` across edits, retracts both records on delete / on leaving PUBLIC. `packages/discovery`: `IndexedPost.collection`/`bskyUri`/`cid`, `applyCommitEvent` handles `app.bsky.feed.post` (behind `INDEX_BSKY_POSTS`, DID-scoped), `mergeIndexedPosts` collapse/dedupe. API: linkage fields on post responses, `PATCH /creators/me/posts/:id` wired, `GET /posts/:id` resolves AT-URI ids, `GET /feed` dedupe guard. Web: `/creator/posts` composer, `/feed`, real `/c/[handle]` Posts tab, `/c/[handle]/post/[id]`, shared `PostCard`. Additive migration `20260902230000_bluesky_public_post_linkage`. Dual-publish still requires `CREATOR_OWNED_PDS_ENABLED`; default runtime unchanged except nullable response fields. `pnpm build` / `-r lint` / `-r typecheck` / `-r test` green (lexicons 29, atproto 17, discovery 39, content 44, api 168, web 109); 14 Playwright e2e green. **Deferred:** public-post media bytes to the PDS (composer is text-only); making dual-publish the default.

**Status (2026-09-02): `creator-owned-pds.md` — protocol research + backend proof-of-concept landed, flag-gated (both flags off by default), paused for privacy review.** `docs/creator-owned-pds.md` is committed (the protocol research with source links and flagged assumptions). Backend PoC: new lexicons `fans.foryour.media` / `accessPolicy` / `serviceConfig` + additive optional fields on `fans.foryour.post`; additive nullable cache columns + `ContentKey`/`ContentKeyGrant` tables (migration `20260902210000_…`); `CreatorOwnedContentRepository` (public dual-publish to the creator's PDS, `rebuildFromPds`, `isAuthoritative` demotion); AES-256-GCM content encryption in `packages/media`; `KeyGrantService` + `POST /content-keys/grant` (entitlement→key boundary, `PENDING`/`PAST_DUE`/`CANCELED`/`EXPIRED`/lapsed all deny); `GET /creators/me/portability`; one-off `migrate:pds` command. **Gated content is DEFERRED per the spec's "Stop and Defer"** — with `CREATOR_OWNED_GATED_CONTENT_ENABLED` off (default), `SUBSCRIBERS`/`TIER` posts stay Postgres-only and app-authoritative, because encrypting into the normal repo archives ciphertext on the firehose forever and atproto Spaces is still alpha (access control, not confidentiality). Not done (implementation phase): media **bytes** off S3, web/composer/feed UI, `packages/discovery` merge/dedupe, production migration. `pnpm build` / `-r lint` / `-r typecheck` / `-r test` all green (api 160, content 40, subscriptions 21, media 33, lexicons 29).

**Build order:** Phases 1–10 (done) → [`prompts/creator-owned-pds.md`](../prompts/creator-owned-pds.md) → [`prompts/bluesky-public-posts.md`](../prompts/bluesky-public-posts.md) → Phases 12–17 (slot 11 vacant) → [`prompts/atproto-spaces.md`](../prompts/atproto-spaces.md) (dead last, experimental, flag-gated). The rearchitecture specs run before 12–17 so Trust & Safety, Hardening, and the Architecture Review are built against the creator-owned-PDS world rather than redone after it.

- [`prompts/creator-owned-pds.md`](../prompts/creator-owned-pds.md) — **rearchitecture.** foryour.fans stops being the system of record for creator content. Creator profiles, tiers, posts, media, and creator-authored config become records/blobs in the *creator's own PDS*; gated (subscriber/tier) content is **encrypted** before it is written there, with foryour.fans coordinating payment → entitlement → decryption-key grants. Postgres/S3 are demoted to caches and rebuildable indexes (`isAuthoritative: false`, `sourceUri`, `indexedAt`, `cacheExpiresAt`). Expands the lexicon set: adds `fans.foryour.media`, `fans.foryour.accessPolicy`, `fans.foryour.serviceConfig`, plus gating/linkage fields on `fans.foryour.post`. Self-gated: "stop and defer" if encrypted-PDS storage can't be made safe with today's protocol surface — ship public creator-owned content only, leave paid/private behind a documented protocol gap, and do **not** keep app-owned storage as the permanent design.
- [`prompts/bluesky-public-posts.md`](../prompts/bluesky-public-posts.md) — **refactor (implemented, flag-gated).** Every `PUBLIC` post authored here is dual-published to the creator's PDS as **both** `app.bsky.feed.post` (so Bluesky and compatible clients render it) **and** `fans.foryour.post` (fan-service metadata), linked by AT URI/CID (one-way, custom → Bluesky). Feeds and discovery merge the two into one item and dedupe. Gated posts are never written as `app.bsky.feed.post`. Still requires `CREATOR_OWNED_PDS_ENABLED`; media bytes to the PDS still deferred.
- [`prompts/atproto-spaces.md`](../prompts/atproto-spaces.md) — **experimental, extracted.** The old Phase 11 / WEB PHASE 11 Spaces adapter (`AtprotoSpacesContentRepository`, `SpaceAuthority`, `ATPROTO_SPACES_ENABLED=false`, dev-only `/dev/spaces`), pulled out of the numbered sequence so the main build has no experimental detour. Runs dead last. By the time it runs, `creator-owned-pds.md` has already made gated content encrypted and creator-PDS-owned, so Spaces is a **key-grant / permission transport** over that storage — never the storage backend — with `canAccess` still the sole authority.

**Order confirmed (2026-09-02): `creator-owned-pds.md`, then `bluesky-public-posts.md`, then `atproto-spaces.md` — dead last.** Pinned in the `prompts/full.md` and `prompts/web.md` preambles. Rationale for the first two: the PDS pivot redraws the lexicon shape and moves where posts live; once `fans.foryour.post` already resides on the creator's PDS, dual-publish is just "also write `app.bsky.feed.post` next to it." Reverse them and the dual-publish plumbing gets built against app-owned storage and reworked when storage moves. Escape hatch: `creator-owned-pds.md` gates itself on a privacy review; if that stalls, `bluesky-public-posts.md` is lower-risk and can still ship against today's architecture on its own. Spaces is last because it is experimental and, post-pivot, only makes sense layered on encrypted creator-owned storage.

**Status (2026-09-03): Phase 12 (Comments, Likes, Social) done.** New `Comment`/`Like` Postgres models (migration `20260903151513_add_comments_and_likes`) and two new `packages/content` modules (`comments.ts`, `likes.ts`) — Postgres-only, never mirrored to AT Protocol for any post visibility (a uniform rule, not visibility-dependent — see `docs/architecture.md`'s Phase 12 section for why). `POST`/`GET /posts/:id/comments` and `POST`/`DELETE /posts/:id/likes` (`apps/api/src/routes/comments.ts`/`likes.ts`) share one new helper, `loadAccessiblePost` (`apps/api/src/routes/posts.ts`), which reuses `GET /posts/:id`'s own `checkPostAccess` entitlement gate so "access inherited from the parent post" is one implementation, not per-route logic — a denied caller gets a real `403` (`GET /posts/:id`'s locked-stub `200` doesn't apply here; there's no safe partial view of a comment thread). Likes are idempotent by construction (`Like.@@unique([postId, userId])` + `upsert`/`deleteMany`), not a pre-check. Incidentally found and fixed while smoke-testing the exit checklist's "app starts" step: `apps/api/src/config/env.ts` used `z.coerce.boolean()` for four flags, which coerces the literal string `"false"` to `true` — meaning a fresh checkout that copied `.env.example` verbatim would have booted with `CREATOR_OWNED_GATED_CONTENT_ENABLED` silently on. Replaced with a real string-to-boolean parser (`booleanEnvFlag`), regression-tested in new `apps/api/test/env.test.ts`, and verified end-to-end (`node apps/api/dist/server.js` against `.env.example`'s unmodified contents now boots, where it previously threw at startup). `pnpm build` / `-r lint` / `-r typecheck` / `-r test` all green (content 60, api 219; 585 tests total across the workspace).

**Status (2026-09-07): Phase 13 (Creator Dashboard) done.** New `packages/subscriptions/src/dashboard.ts` — `getCreatorDashboard`, a pure analytics function over the existing `Subscription`/`SubscriptionTier` tables (no new models, no migration). `subscriberCount` (PENDING/ACTIVE/PAST_DUE) and `activeSubscriptions` (ACTIVE only) are deliberately distinct; `mrrCents`/`revenueByTier` sum each subscription's `priceCentsAtSubscription` snapshot, never a tier's live price, continuing the grandfathering discipline Phase 6 established. Only `newSubscribers`/`cancellations`/the daily `timeSeries` are bounded by the caller's date range (`normalizeDashboardRange`, UTC day boundaries, 366-day cap) — the other fields are always a right-now snapshot, since a past-dated MRR figure isn't a meaningful question for a live-state concept. The time series is an honest, documented approximation: `Subscription` has no full status-transition log, only `createdAt`/current status/`updatedAt`, so a subscription is treated as continuously active from `createdAt` until (if it has since ended) `updatedAt` — a `PAST_DUE`-then-recovered subscription doesn't dip out of the chart. `GET /creators/me/dashboard` (`apps/api/src/routes/dashboard.ts`) resolves the creator from the session DID exactly like every other `/creators/me/*` route (never a client-supplied id), composes in payout status (`getPayoutAccountStatus`, reused from Phase 6) and the creator's 5 most recent posts (`ContentRepository.getCreatorFeed`) — payout gating of the actual dollar figures is left to the web UI, not enforced server-side (the API always returns real numbers; `/creator/payouts`'s own precedent already treats "verified" as gating what's displayed, not what a creator can have). `pnpm build`/`-r lint`/`-r typecheck`/`-r test` all green (subscriptions +10 tests, api +8 tests; no migration).

### What changes in Phases 12–17 (reconciled via the `prompts/full.md` preamble)

| Phase | Assumption today | After the two rearchitecture specs |
|---|---|---|
| 12 Comments/Likes | app-owned `Comment` / `Like`; access inherited from post | Unchanged for gated posts. Open question: do interactions on a dual-published **public** post also get a Bluesky-native (`app.bsky.feed.like` / reply) representation? Out of scope for that phase — flag it. |
| 13 Dashboard | billing DB authoritative, not AT | Reinforced — payment-coordination tables stay app-side; subscriber counts / MRR never come from PDS records. |
| 14 Trust & Safety | "remove content" deletes the record | App can deindex, tombstone its cache, and **revoke key grants**; it cannot delete the creator's PDS record. NCII / illegal-content handling against encrypted content is a design item for legal review. |
| 15 Hardening | bypass signed URLs / tier restrictions | Also: obtain decryption keys without active entitlement, replay expired key grants, decrypt gated blobs pulled straight from the PDS. Add key-at-rest and key-rotation review. |
| 16 Kubernetes | "S3 configuration" | S3 is staging/cache only; add creator-PDS connectivity and (if keys are server/KMS-generated) a KMS/secret backend. The `ingest.ts` process also ingests `app.bsky.feed.post`. |
| 17 Architecture Review | 5 diagrams as listed | "Paid content" diagram becomes creator → encrypted PDS record + blob → entitlement check → key grant → subscriber; add a data-portability diagram; the "Future Spaces" diagram becomes key-grant transport, not storage. |

`docs/creator-owned-pds.md` (protocol research output) and `docs/bluesky-public-posts.md` (lexicon-field research output) are deliverables **of** those phases — each spec's "Required Research Before Implementation" section must be committed before its implementation starts. `docs/creator-owned-pds.md` **is committed** (see the status note above); `docs/bluesky-public-posts.md` is a stub until `prompts/bluesky-public-posts.md` runs.

## Web UI build plan

`prompts/web.md` is the frontend counterpart to `prompts/full.md`. It turns the current bare `apps/web` skeleton (unstyled App Router, a handful of routes) into a full product UI: marketing site, authenticated app, creator tooling, and a role-gated moderation console. Same conventions as `full.md` — imperative phases, per-phase exit checklist, explicit `Stop after` lines, run each phase as its own Codex session.

**Web status (2026-09-01): WEB PHASES 0–4 done.** Phase 0: Tailwind + CSS-variable design tokens (light/dark, `ff_theme` cookie, no-flash SSR), ~20 UI primitives in `apps/web/components/ui/` wrapping Radix, `(marketing)`/`(app)` route groups with a server-resolved two-variant `Header`, `SessionProvider`, `(app)` anon→`/login?next=` redirect, global `error.tsx` / refreshed `not-found.tsx`, dev-only `/dev/components`, `docs/ux.md`. Phase 1: public marketing site — `/` (logged-out hero + explainer sections + `Featured creators` `EmptyState`; logged-in personalized panel, no redirect), `/about`, legal placeholders, `/discover` teaser, SEO. Phase 2: auth UX — validated `/login`, `/auth/callback` interstitial (new-vs-returning routing, safe `next`, cancelled/failed-sign-in copy), shared `logout()`, `apiFetch` 401→`/login` session-expiry wrapper, Playwright round-trip. Phase 3: `/settings` — `Account` (AT-vs-app field provenance, copyable immutable DID, "Refresh from AT Protocol"), `Appearance` (theme override → `ff_theme`, live), `Notifications` (disabled preview), inert deactivation card. Phase 4: creator onboarding — 4-step `/become-a-creator` wizard (slug rules + live availability, profile, self-attested content-rating *placeholder*, review → `POST /creators`), public `/c/:slug` (handle/slug/DID, owner Edit, `EmptyState` tiers/posts, disabled Subscribe), rebuilt `/creator/settings` with a guarded slug-change `Dialog`. **API changes:** WEB PHASE 2 pointed `GET /auth/atproto/callback` at `<PUBLIC_URL>/auth/callback` (± `?error=`); WEB PHASE 3 added `POST /me/refresh`; **WEB PHASE 4 needed none.** Avatar/banner upload (no blob path → WEB PHASE 8) and content-rating persistence (no field → WEB PHASE 14) are marked placeholders. `apps/web` Vitest + RTL 50 tests; Playwright 7 tests (fake-OAuth API in `apps/api/test/e2e/fakeServer.ts`; CI wiring = WEB PHASE 16); the web-track auth changes are covered by `apps/api/test/auth.test.ts`. WEB PHASES 5–17 not started; 5 unblocked (API Phase 5 shipped).

**Web status (2026-09-03): WEB PHASE 8 (media upload & rendering UX) done.** Composer `MediaUploader` (`apps/web/components/media/`): drag-and-drop + file picker, client MIME/size validation *before* any presigned URL (mirrors `@foryour-fans/media`'s lists), a real progress bar on the direct-to-storage `PUT` (`XMLHttpRequest`, since `fetch` has no upload-progress event), a status poll (`processing` spinner → `ready` thumbnail / `rejected` reason), and a `@dnd-kit`-reorderable list whose order maps 1:1 to `PostMedia.sortOrder`. The composer blocks **Publish** until every attachment is `ready` and seeds the list from an edited post's existing attachments. Rendering: `useSignedMedia` resolves bytes through `GET /media/:id/access` on demand and refreshes the short-lived URL before expiry (never embeds a storage URL); `MediaGallery` + keyboard-navigable `MediaLightbox` on the single-post view, `MediaThumb` (first attachment + "+N") on feed cards. NSFW media is blurred by default with a per-item **Reveal** — the *mechanism* only; there is no content-label API before WEB PHASE 14, so `nsfw` defaults off. **Thin web-track API additions** (precedent: WEB PHASE 5 / 7): `POST`/`PATCH /creators/me/posts` accept `media: [{mediaAssetId, sortOrder}]` (validated in `@foryour-fans/content`'s new `resolvePostMedia` — READY, creator-owned, ≤20, bounded/deduped/dense `sortOrder` — → 400 on any failure), and `GET /media/:id/access` no longer grants "any active subscriber": the asset's creator always, otherwise the viewer must be able to read at least one post the asset is attached to (same `checkPostAccess` as `GET /posts/:id`), unattached = creator-only, non-`READY` = never. A new owner-only `GET /media/:id` backs the composer's ready-gate poll. `PostRecord.media` now carries `mimeType` + intrinsic dimensions/duration (layout metadata, never a storage key). No migration — `PostMedia` already existed. `pnpm build` / `-r lint` / `-r typecheck` / `-r test` green (web 145, api 187, content 47); 20 Playwright e2e (fakeServer gained an in-process object store over `/__e2e__/blob/*` so the presigned-PUT → signed-GET round trip runs for real). **Deferred:** public-post media *bytes* to the creator's PDS (still app object storage only — a `creator-owned-pds.md` item); real NSFW labelling (WEB PHASE 14).

**Web status (2026-09-03): WEB PHASE 9 (Feeds) done.** Most of this phase's surface — `FeedList`, `CreatorFeed`, `PostCard`, `lib/post.ts#postBadges`, `LockedPostCard` — had already landed early as part of `bluesky-public-posts.md`'s web half (see that phase's entry below); WEB PHASE 9 closed the three gaps still open against the spec. **`/feed` moved from `(app)` to `(marketing)`**: an anonymous visit is now a chosen answer — a `FeedLoggedOut` explainer (`Log in` / `Browse creators` CTAs) — instead of the `(app)` group's `/login?next=` redirect, and instead of the real PUBLIC-only stream the API would actually serve one; the signed-in empty state now links to `/discover`. **A fifth, previously-missing card state**: `postBadges()` now appends a **Subscribed** badge to an unlocked `SUBSCRIBERS`/`TIER` post when the viewer isn't its owner (`viewerIsOwner` param, threaded through `PostCard` from `CreatorFeed`'s existing `isOwner`) — `Public`, `Subscriber-only`, `Tier`, and `Locked` were already visually distinct via badge label + body presence. **Prev/next navigation** on `/c/[handle]/post/[id]`: a new pure `findFeedNeighbors` helper locates the post within one `limit=50` page of `GET /creators/:id/feed` (there's no dedicated "neighbors" route); both `PostArticle` and `LockedPostCard` render the resulting `PostNav`, and a post older than that window just gets no nav — an accepted degradation, not a bug. **No API changes** — WEB PHASE 9 consumes `GET /feed` and `GET /creators/:identifier/feed` exactly as Phase 9 shipped them. `apps/web` Vitest + RTL 152 tests (was 145; +1 `findFeedNeighbors` suite, +3 `postBadges`/`PostCard` "Subscribed" cases, +1 `FeedList` discover-CTA case); Playwright +3 (`e2e/feed.spec.ts`: anonymous explainer, signed-in feed, next/prev nav) on top of the existing 20. `pnpm --filter @foryour-fans/web build`/`lint`/`typecheck`/`test` green.

**Web status (2026-09-03): WEB PHASE 10 (Discovery & Search) done.** `/discover` (browse) and a new `/search?q=` route share one client component, `components/discover/DiscoverBrowser.tsx`, seeded server-side with a different first page each (`GET /discover` vs `GET /search`) — a debounced (350ms) search input re-fetches client-side and mirrors the query into the URL via `history.replaceState`, never a Next navigation, so typing never remounts the page or double-fetches; recent searches persist to `localStorage` only, per spec. Results render as a cursor-paginated grid of the new `components/discover/CreatorCard.tsx` through `InfiniteList` — a WEB PHASE 0 primitive that had never had a real consumer until now. **Thin web-track API addition** (same precedent as WEB PHASE 5/7/8): `GET /discover`/`GET /search` (`apps/api/src/routes/discovery.ts`) now also return `avatarUrl`, `tierCount`, and `fromPriceCents`/`fromPriceCurrency` (the cheapest active tier) — sourced from the local `Creator`/`SubscriptionTier`/`User` tables for the registered DIDs the route already looked up for `isRegisteredCreator`, `null`/`0` for an indexed-but-unregistered profile (the index is genuinely network-wide — any DID publishing `fans.foryour.profile`, not only ones that have signed in here). `CreatorCard` uses that flag to decide whether a card links to `/c/:handle` at all — an unregistered result renders inert with a "Not on foryour.fans yet" badge instead of a click-through to a 404. The home page's `Featured creators` strip (stubbed since WEB PHASE 1) now renders the first page of `GET /discover` through the same `CreatorCard`, with a "See all creators" link to `/discover`; it still falls back to the original `EmptyState` copy when the index is genuinely empty — never fake creators. **Two spec bullets are honest, documented reductions, not oversights**: `full.md` PHASE 10 never defines a creator "category" concept and the index has exactly one meaningful ordering (`indexedAt desc`), so `/discover` ships one real grid in that order rather than inventing a "new"/"active"/"by category" taxonomy the backend can't support; and NSFW/age-gating is left off both routes because no content-rating field exists anywhere in the schema yet (the same placeholder gap `/become-a-creator`'s content-rating step has carried since WEB PHASE 4, still unclosed) — gating on a field that doesn't exist would mean fabricating client-only state with nothing real behind it. Both are written up in `docs/ux.md`'s "Known limitations after WEB PHASE 10". `apps/web` Vitest + RTL 164 tests (was 152; +12: `lib/discover.ts`, `CreatorCard`, `DiscoverBrowser`, +1 more `FeaturedCreators` case) — the suite's `vitest.setup.ts` also gained an `IntersectionObserver` stub since `InfiniteList` is now actually rendered in tests. Playwright +3 (`e2e/discover.spec.ts`) on top of the existing 23, with a new seeded, indexed, tiered creator in `apps/api/test/e2e/fakeServer.ts` so the enrichment path (registered + tier count + from-price) is exercised for real, not just the unregistered/empty cases. `apps/api` gained one enrichment test in `test/discovery.test.ts` (188 total, was 187). `pnpm -r build`/`lint`/`typecheck`/`test` green.

**Web status (2026-09-03): WEB PHASE 12 (Comments & Likes) done.** `/c/[handle]/post/[id]`'s `PostArticle` (never `LockedPostCard` — access inherited from the post by construction) gains a `LikeButton` and `CommentThread`. `LikeButton`: optimistic toggle with rollback-on-error (a failed `POST`/`DELETE /posts/:id/likes` restores the prior count/pressed-state and shows an error toast), an anonymous viewer gets a login link instead of a toggle, and a "Liked by `<creator>`" indicator (skipped for the creator's own view of their own like, mirroring `postBadges()`'s `viewerIsOwner` precedent for the "Subscribed" badge). `CommentThread`: server-rendered first page, oldest-first, cursor-paginated via `InfiniteList` (the same fetch-and-append pattern WEB PHASE 10's `DiscoverBrowser` established); `CommentComposer` for a signed-in viewer, a "log in to comment" prompt otherwise (the backend requires a session to comment on *any* post, `PUBLIC` included, so an anonymous composer would just `401` on submit); the post's own creator's comments badged **Creator**; a Report `Flag` entry point per comment shows a "not available yet" toast — the dialog itself is WEB PHASE 14's job, per the spec's own words. **Thin web-track API additions** (same precedent as WEB PHASE 5/7/8/10, and refinements to Phase 12's own API rather than scope creep into a later phase — see `docs/architecture.md`'s Phase 12 section): `GET /posts/:id`'s unlocked response gains `likeCount`/`likedByViewer`/`likedByCreator` (`getLikeSummary`, `packages/content/src/likes.ts` — there's still no dedicated `GET /posts/:id/likes` route); `GET /posts/:id/comments` changed shape from a bare array (had no real consumer before this phase) to `{comments, nextCursor}`, matching `GET /discover`'s own cursor convention. **A real, pre-existing test-suite ordering bug was found and fixed**: `apps/api/test/e2e/fakeServer.ts`'s startup cleanup deleted the fixture creator's posts before this phase's new `Like`/`Comment` rows (both FK-`RESTRICT` onto `Post`, same as `PostMedia`), which only surfaced once a Playwright spec actually created a like/comment — fixed the same way `apps/api/test/helpers.ts#cleanupUser` already had to be, by clearing both tables (scoped both ways: an actor's own rows, and rows on a seeded identity's own posts) before `Post.deleteMany`. Separately, the new e2e spec file had to be named to sort after `creator.spec.ts` (`e2e/social.spec.ts`, not `comments-likes.spec.ts`) — Playwright runs spec files in filename order in this suite (`workers: 1`, `fullyParallel: false`), and `creator.spec.ts`'s first test assumes the fixture identity isn't a creator yet, an assumption every file that calls the shared `ensureFixtureIsCreator()` helper (this one included) breaks if it sorts first alphabetically. `apps/web` Vitest + RTL 186 tests (was 164; +22: `LikeButton`, `CommentThread`, `lib/comments.ts`, `lib/likes.ts`); Playwright +4 (`e2e/social.spec.ts`) on top of the existing 26 (27 total, including one pre-existing, unrelated flake in `creator.spec.ts` reproduced in isolation — a Playwright strict-mode locator ambiguity between a toast and static page text, not caused by this phase). `apps/api` gained 6 tests (env-parsing-bug regressions aside — see backend Phase 12 above) for the like-summary/comments-shape changes (content 64, api 221). `pnpm -r build`/`lint`/`typecheck`/`test` green.

**Web status (2026-09-07): WEB PHASE 13 (Creator Dashboard) done.** `/creator/dashboard` — no API changes, consumes `GET /creators/me/dashboard` exactly as backend Phase 13 shipped it. Four always-visible stat tiles (Subscribers, Active subscriptions, New subscribers, Cancellations); a Revenue card (MRR + a ranked inline-bar revenue-by-tier list) and an MRR-over-time chart wrapped in a new `PayoutGate` component (masked behind a "complete payout onboarding to see earnings" prompt until payout status is `VERIFIED` — subscriber/engagement numbers are never gated, only money figures, matching how `/creator/payouts` already treats verification as a display gate, not an access gate); a `DateRangeFilter` (7/30/90-day presets + custom `<input type="date">` start/end) that re-fetches client-side on change (the initial 30-day page is server-rendered); two single-series `recharts` area charts (Subscribers over time, MRR over time — `recharts` chosen per the spec's "pick the chart lib here"; one series per chart needs no legend, per the data-viz method, since the card title already names it; uses the app's own `--primary` token rather than a separate charting palette); a Recent Posts list (last 5, any visibility); and a `NewCreatorGuidance` checklist replacing the all-zero-looking dashboard for a creator with zero tiers, zero posts, and no subscriber ever. **A real bug found via the e2e suite, not just inspection**: the guidance checklist's "publish a post" CTA and the empty recent-posts list's own CTA both read "Write a post" — a duplicate accessible name that surfaces as a genuine Playwright strict-mode violation whenever both render together (a brand-new creator — which the shared e2e fixture identity actually still was when this spec ran, since an unrelated pre-existing flake in `creator.spec.ts` skips the tier-creation test that would otherwise have given it a tier) — fixed by renaming the recent-posts-empty-state CTA to "New post". `apps/web` Vitest + RTL 219 tests (was 186; +33 for the dashboard: `lib/dashboard.ts`'s preset-math/`isNewCreator` helpers, `DateRangeFilter`, `PayoutGate`, `NewCreatorGuidance`, `RevenueByTierList`, `RecentPostsList`, a `TimeSeriesChart` smoke test — `recharts`' `ResponsiveContainer` renders 0×0 under jsdom, so this asserts it renders without throwing plus the right `role="img"`/`aria-label`, not geometry — and `DashboardClient`, covering the payout-masking/guidance-panel/date-range-refetch/error-retry behavior). Playwright +1 (`e2e/dashboard.spec.ts`) on top of the existing 30 (31 total) — named to sort right after `creator.spec.ts` and before `discover`/`posts`/`social`/`subscribe`, so it isn't racing those later specs for "payout onboarding not started yet"/"no posts published yet" to still be true, the same filename-order discipline `social.spec.ts` documented for WEB PHASE 12. `pnpm -r build`/`lint`/`typecheck`/`test` green.

Key constraints baked into that spec:

- **It never invents an API.** Each `WEB PHASE n` consumes only what `full.md`'s `PHASE n` ships, and must not start before those routes exist. `WEB PHASE 0`/`1` are the exception — no backend dependency beyond the shipped Phases 1–4.
- **Product scope is pinned to `full.md`.** OnlyFans-style features that `full.md` does *not* cover — direct messaging, pay-per-view/unlockable posts, tipping, live streaming, stories, native mobile apps — are explicitly out of scope for every web phase. Adding them means a new `full.md` phase first.
- **Cross-cutting requirements enforced every phase:** an auth/role state matrix per route (anon / authed non-creator / creator / subscriber / admin), distinct logged-out vs logged-in app shell resolved server-side (no flicker), `loading`/`empty`/`error`/`locked` states on every data view, protected post body/media never sent to the client, NSFW age-gate + blur-by-default + no NSFW in OG/logged-out surfaces, WCAG AA baseline, cursor pagination only, no secrets in the bundle.

| Web phase | Consumes API from | Builds |
|-----------|-------------------|--------|
| 0 | Phases 1–3 (done) | Design tokens/theming, UI primitives, `(marketing)`/`(app)` route groups, `SessionProvider`, `docs/ux.md` |
| 1 | Phase 2 (done) | Landing page (logged-out hero + logged-in personalized panel), `/about`, legal stubs, `/discover` teaser |
| 2 | Phase 2 (done) | Real `/login`, callback screen, session-expiry handling, logout; introduces Playwright |
| 3 | Phases 2–3 (done) | `/settings` (handle/DID/avatar/banner, theme override, notifications placeholder) |
| 4 | Phase 4 (done; slug flow later removed) | `/become-a-creator` wizard, public `/c/[handle]` (was `/c/:slug`) |
| 5 | Phase 5 (done) | `/creator/tiers` CRUD + reorder, grandfathering messaging, public tier cards |
| 6 | Phase 6 (done) | Subscribe flow (hosted-checkout redirect model), `/subscriptions`, `/creator/payouts` onboarding |
| 7 | Phase 7 (done) | `/creator/posts` + composer with visibility selector and public-vs-private warning; locked single-post view |
| 8 | Phase 8 (done) | Drag-drop uploader (presigned URL, status polling), on-demand signed-URL media rendering, NSFW blur/lightbox |
| 9 | Phase 9 (done) | `/feed`, creator feed tab, locked-post preview cards, unlock-state styling |
| 10 | Phase 10 (done) | `/discover` browse, `/search`, DID-stable creator links, real featured strip on `/` |
| ~~11~~ | — | Spaces UI **extracted** to [`prompts/atproto-spaces.md`](../prompts/atproto-spaces.md); runs dead last. No user-facing UI — storage backend stays invisible; optional dev-only `/dev/spaces` diagnostics behind `ATPROTO_SPACES_ENABLED` |
| 12 | Phase 12 | Comment threads (access inherited from post), optimistic likes, report entry points |
| 13 | Phase 13 | `/creator/dashboard` — subscriber/revenue analytics, charts, date filters, payout-status widget |
| 14 | Phase 14 | Report/block dialogs, age + creator KYC verification flows (gate posting/payouts), content-label reveal, role-gated `/admin` moderation console + audit log |
| 15 | Phase 15 | Error boundaries, a11y audit, performance/bundle pass, responsive pass, `docs/web-accessibility.md` |
| 16 | Phase 16 | `output: "standalone"` + Dockerfile, typed env config, web-tier CSP/security headers, k8s Deployment + probes, CI extension |
| 17 | Phase 17 | `docs/ux-review.md` — screen inventory, full state matrix, flow diagrams, risk list, MVP-readiness classification. No new features |

## Suggested next step

Phases 1–10, the Handle-as-Identity refactor, both post-Phase-10 rearchitecture specs, Phase 12 (Comments, Likes, Social), and Phase 13 (Creator Dashboard) are all done (see the status notes above). The old Phase 11 slot stays vacant — AT Protocol Spaces was extracted to [`prompts/atproto-spaces.md`](../prompts/atproto-spaces.md), which runs dead last.

Start **Phase 14 — Trust and Safety Foundation** next (`Report`/`ModerationCase`/`ContentLabel`/`UserBlock`/`CreatorBlock`/`AuditLog` models; report-creator/report-post/report-comment/block-user actions; admin moderation — review reported content, restrict account, remove content, suspend creator, all with audit logging; pluggable interfaces for future automated classifiers, not a hard-coded provider; a `Creator.verificationStatus` field gating real `PayoutProvider` onboarding and adult-content tier/post marking, per `prompts/full.md`'s content-policy note — do not invent legal-compliance requirements beyond what the spec asks, but do document where legal/compliance review is required). Each phase should be run as its own Codex session against the corresponding section of `prompts/full.md`, stopping where that phase says to stop — do not let a session continue into the next phase's scope even if it seems trivial to keep going.

The web track can proceed in parallel: `WEB PHASE 0`–`10`, `WEB PHASE 12`, and `WEB PHASE 13` are done (`WEB PHASE 11` is the same vacant Spaces slot); `WEB PHASE 14` (report/block dialogs, age + creator KYC verification flows, content-label reveal, a role-gated `/admin` moderation console) is unblocked once backend Phase 14 ships. Run web phases as their own Codex sessions against `prompts/web.md`, same stop-at-the-line discipline.
